<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sheltersheets">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".NumberPickerFragment"></activity>
        <activity
            android:name=".DonorHome"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".DonorProfile"
            android:parentActivityName=".DonorHome"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.sheltersheets.DonorHome" />
        </activity>
        <activity
            android:name=".CreateProfileNow"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".NewUserUI"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>